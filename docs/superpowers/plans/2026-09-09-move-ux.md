# 移动功能 UX 优化实现计划（搜索 + 树弹窗）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用「搜索框 + 保留树结构的模态弹窗」替代当前平铺 108 项的二级右键菜单来选择移动目标文件夹，选中后点确认移动并弹 toast 提示。

**Architecture:** 纯前端改动，后端复用现有 `media_update`。新增两个组件（`MoveDialog.ts` 模态弹窗、`Toast.ts` 轻提示）和一个可测纯函数（`moveTreeFilter.ts` 的 `matchTree`）。`VideoView.ts` 的「移动」菜单项从平铺映射改为打开弹窗。样式加到 `src/styles/theme.css`。

**Tech Stack:** Tauri 2 + vanilla-ts + Vite。无测试框架（`npm run build` = `tsc && vite build`），纯函数验证用独立 `tsx` 脚本 + `tsc` 类型检查，真机验证跑 `npm run tauri dev`。

**规范约定（务必遵守）:**
- `category_path` 是「分类内相对路径」，不含分类名；树根 `path` 为空串 `""`。
- 移动仅在当前分类内，`category` 不变，只改 `category_path`。
- `api.mediaUpdate` 签名无 path 参数：`mediaUpdate(id, category, categoryPath, title, subtitlePath, coverPath, description)`。
- 现有 CSS 变量：`--glass` `--glass-strong` `--border` `--accent` `--accent-glow` `--text` `--text-dim`。已有 `.btn` `.btn-primary` 可复用。
- 现有树行结构参照 `src/components/TreeView.ts` 的 `renderTreeNodes`（箭头 `▾/▸/　`，`padding-left = depth*14+4`）。

---

### Task 1: 树过滤纯函数 `matchTree`

**Files:**
- Create: `src/lib/moveTreeFilter.ts`
- Test: `scripts/test-moveTreeFilter.ts`（独立可执行验证脚本）

保留树结构的搜索过滤核心：给定节点和查询串，返回该子树里哪些节点可见（命中项及其所有祖先、命中项本身的后代不强制可见——只显示到命中层）。规则：节点可见 ⇔ 它自身 path 含 query 或它有任一后代可见（大小写不敏感，按 path 全串 `includes`）。query 为空时全可见。同时返回「因命中而需强制展开」的节点集（命中项的祖先），供弹窗渲染时无视 expanded 状态展开。

- [ ] **Step 1: 写验证脚本（先失败——文件不存在）**

Create `scripts/test-moveTreeFilter.ts`:

```ts
import { matchTree } from "../src/lib/moveTreeFilter";
import type { TreeNode } from "../src/lib/videoTree";

function node(name: string, path: string, children: TreeNode[] = []): TreeNode {
  return { name, path, children, items: [] };
}

// 树： 电影(根"") ─ 科幻/星战, 科幻/沙丘, 奇幻/哈利波特
const root: TreeNode = node("电影", "", [
  node("科幻", "科幻", [ node("星战", "科幻/星战"), node("沙丘", "科幻/沙丘") ]),
  node("奇幻", "奇幻", [ node("哈利波特", "奇幻/哈利波特") ]),
]);

let failed = 0;
function assert(cond: boolean, msg: string) {
  if (!cond) { console.error("FAIL:", msg); failed++; } else { console.log("ok:", msg); }
}

// 空 query：全可见
{
  const r = matchTree(root, "");
  assert(r.visible.has("") && r.visible.has("科幻") && r.visible.has("科幻/星战") && r.visible.has("奇幻/哈利波特"),
    "空query全部可见");
}

// 命中叶子「星战」：其祖先(根、科幻)可见且强制展开；无关分支(奇幻子树)不可见
{
  const r = matchTree(root, "星战");
  assert(r.visible.has("科幻/星战"), "命中项可见");
  assert(r.visible.has("科幻") && r.visible.has(""), "命中项祖先可见");
  assert(r.forceExpand.has("科幻") && r.forceExpand.has(""), "命中项祖先强制展开");
  assert(!r.visible.has("科幻/沙丘"), "同级未命中项不可见");
  assert(!r.visible.has("奇幻") && !r.visible.has("奇幻/哈利波特"), "无关分支不可见");
}

// 命中中间节点「科幻」：科幻及其所有后代可见（父级含 query 即整枝可见）
{
  const r = matchTree(root, "科幻");
  assert(r.visible.has("科幻") && r.visible.has("科幻/星战") && r.visible.has("科幻/沙丘"),
    "命中中间节点其整枝可见");
  assert(!r.visible.has("奇幻"), "无关分支不可见");
}

// 大小写不敏感（用英文路径验证）
{
  const en: TreeNode = node("Movie", "", [ node("SciFi", "SciFi", [ node("Dune", "SciFi/Dune") ]) ]);
  const r = matchTree(en, "dune");
  assert(r.visible.has("SciFi/Dune"), "大小写不敏感命中");
}

if (failed) { console.error(`\n${failed} 个断言失败`); process.exit(1); }
console.log("\n全部通过");
```

- [ ] **Step 2: 运行验证它失败**

Run: `cd /Users/zhoumo/Documents/Claude/collector && npx tsx scripts/test-moveTreeFilter.ts`
Expected: 报错——找不到模块 `../src/lib/moveTreeFilter`（文件尚未创建）。

- [ ] **Step 3: 写实现**

Create `src/lib/moveTreeFilter.ts`:

```ts
import type { TreeNode } from "./videoTree";

export interface MatchResult {
  visible: Set<string>;      // 可见节点的 path 集
  forceExpand: Set<string>;  // 因命中需强制展开的节点 path 集（命中项的祖先）
}

/**
 * 保留树结构的搜索过滤：
 * - query 为空：所有节点可见，forceExpand 为空（按 expanded 正常展开）。
 * - query 非空（大小写不敏感，按 path 全串 includes）：
 *   节点可见 ⇔ 自身 path 含 query（此时其整棵子树也一并可见）或任一后代可见。
 *   命中项的祖先加入 forceExpand，使渲染时无视 expanded 强制展开、露出命中项。
 */
export function matchTree(root: TreeNode, query: string): MatchResult {
  const visible = new Set<string>();
  const forceExpand = new Set<string>();
  const q = query.trim().toLowerCase();

  if (!q) {
    const all = (n: TreeNode) => { visible.add(n.path); n.children.forEach(all); };
    all(root);
    return { visible, forceExpand };
  }

  // 返回该节点子树是否含可见项；selfHit 表示祖先中已有命中（含自身），令整枝可见。
  const walk = (node: TreeNode, ancestorHit: boolean): boolean => {
    const selfHit = node.path.toLowerCase().includes(q);
    const hit = ancestorHit || selfHit;
    let descVisible = false;
    for (const c of node.children) {
      if (walk(c, hit)) descVisible = true;
    }
    const nodeVisible = hit || descVisible;
    if (nodeVisible) visible.add(node.path);
    // 有可见后代但自身非命中终点 → 需展开以露出后代
    if (descVisible) forceExpand.add(node.path);
    return nodeVisible;
  };
  walk(root, false);
  return { visible, forceExpand };
}
```

- [ ] **Step 4: 运行验证通过**

Run: `cd /Users/zhoumo/Documents/Claude/collector && npx tsx scripts/test-moveTreeFilter.ts`
Expected: 输出多行 `ok:` 且末尾 `全部通过`，退出码 0。

- [ ] **Step 5: 提交**

```bash
cd /Users/zhoumo/Documents/Claude/collector
git add src/lib/moveTreeFilter.ts scripts/test-moveTreeFilter.ts
git commit -m "feat: 移动弹窗树过滤纯函数 matchTree(保留树结构)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: Toast 轻提示组件

**Files:**
- Create: `src/components/Toast.ts`
- Modify: `src/styles/theme.css`（文件末尾追加 toast 样式）

底部居中的一次性提示，2.5s 后淡出移除；同一时刻只保留一个（新调用先清旧的）。

- [ ] **Step 1: 写实现**

Create `src/components/Toast.ts`:

```ts
/**
 * 屏幕底部居中的轻提示。同一时刻只保留一个（新 toast 先移除旧的）。
 * 2.5s 后淡出并移除。kind="error" 用红色变体。
 */
export function showToast(message: string, kind: "info" | "error" = "info"): void {
  document.querySelectorAll(".toast").forEach((t) => t.remove());
  const el = document.createElement("div");
  el.className = "toast glass" + (kind === "error" ? " toast-error" : "");
  el.textContent = message;
  document.body.appendChild(el);
  // 触发进入动画
  requestAnimationFrame(() => el.classList.add("toast-show"));
  setTimeout(() => {
    el.classList.remove("toast-show");
    setTimeout(() => el.remove(), 300); // 等淡出动画结束
  }, 2500);
}
```

- [ ] **Step 2: 追加样式到 `src/styles/theme.css` 末尾**

```css
/* Toast 轻提示 */
.toast{position:fixed;left:50%;bottom:32px;transform:translate(-50%,12px);z-index:300;
  padding:10px 18px;border-radius:10px;font-size:13px;color:var(--text);
  max-width:70vw;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
  opacity:0;transition:opacity .3s,transform .3s;pointer-events:none}
.toast.toast-show{opacity:1;transform:translate(-50%,0)}
.toast-error{border-color:#e06a6a;color:#ffd6d6}
```

- [ ] **Step 3: 类型检查通过**

Run: `cd /Users/zhoumo/Documents/Claude/collector && npx tsc --noEmit`
Expected: 无错误（退出码 0）。

- [ ] **Step 4: 提交**

```bash
cd /Users/zhoumo/Documents/Claude/collector
git add src/components/Toast.ts src/styles/theme.css
git commit -m "feat: 通用 Toast 轻提示组件

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: MoveDialog 模态弹窗组件

**Files:**
- Create: `src/components/MoveDialog.ts`
- Modify: `src/styles/theme.css`（追加弹窗样式）

搜索 + 保留树结构的模态弹窗。依赖 Task 1 的 `matchTree`、Task 2 的 `showToast`。

- [ ] **Step 1: 写实现**

Create `src/components/MoveDialog.ts`:

```ts
import type { MediaItem } from "../lib/ipc";
import { api } from "../lib/ipc";
import type { TreeNode } from "../lib/videoTree";
import { findNode } from "../lib/videoTree";
import { matchTree } from "../lib/moveTreeFilter";
import { showToast } from "./Toast";
import { esc } from "../lib/escape";
import { displayTitle } from "../lib/displayTitle";

/**
 * 打开「移动到目标文件夹」模态弹窗。
 * item: 被移动视频；tree: 当前分类完整目录树（buildVideoTree 产出，根 path=""）；
 * onMoved: 移动成功后回调（通常为 VideoView 的 refresh）。
 */
export function openMoveDialog(
  item: MediaItem,
  tree: TreeNode,
  onMoved: () => void
): void {
  const expanded = new Set<string>([tree.path]); // 初始只展开根
  let query = "";
  let selected: string | null = null;

  const overlay = document.createElement("div");
  overlay.className = "modal-overlay";
  const dialog = document.createElement("div");
  dialog.className = "move-dialog glass";
  overlay.appendChild(dialog);

  const close = () => {
    overlay.remove();
    document.removeEventListener("keydown", onKey, true);
  };
  const onKey = (e: KeyboardEvent) => { if (e.key === "Escape") close(); };

  // 选中目标的显示名：根用分类名，否则用节点末段名
  const targetName = (): string => {
    if (selected === null) return "";
    if (selected === tree.path) return tree.name;
    const n = findNode(tree, selected);
    return n ? n.name : selected;
  };

  const render = () => {
    const { visible, forceExpand } = matchTree(tree, query);

    const renderNode = (node: TreeNode, depth: number): string => {
      if (!visible.has(node.path)) return "";
      const isRoot = node.path === tree.path;
      const isCur = node.path === item.category_path;
      const hasChildren = node.children.length > 0;
      const isOpen = query ? forceExpand.has(node.path) : expanded.has(node.path);
      const arrow = hasChildren ? (isOpen ? "▾" : "▸") : "　";
      const name = isRoot ? `${node.name}（根）` : node.name;
      const cls = "move-node" + (selected === node.path ? " sel" : "") + (isCur ? " cur" : "");
      const tag = isCur ? `<span class="move-tag">当前位置</span>` : "";
      const row = `<div class="${cls}" data-path="${esc(node.path)}" style="padding-left:${depth * 14 + 4}px">
        <span class="move-arrow" data-toggle="${esc(node.path)}">${arrow}</span>
        <span class="move-name">${esc(name)}</span>${tag}
      </div>`;
      const childrenHtml = isOpen ? node.children.map(c => renderNode(c, depth + 1)).join("") : "";
      return row + childrenHtml;
    };

    const canMove = selected !== null;
    const moveLabel = canMove ? `移动到「${esc(targetName())}」` : "请选择目标文件夹";

    dialog.innerHTML = `
      <div class="move-title">移动 <b>「${esc(displayTitle(item.title))}」</b> 到…</div>
      <input class="move-search" placeholder="🔍 搜索文件夹" value="${esc(query)}"/>
      <div class="move-tree">${renderNode(tree, 0)}</div>
      <div class="move-hint">搜索缩小范围；点箭头展开/折叠；点名称选中目标</div>
      <div class="move-actions">
        <button class="btn move-cancel">取消</button>
        <button class="btn btn-primary move-confirm${canMove ? "" : " disabled"}">${moveLabel}</button>
      </div>`;

    const search = dialog.querySelector<HTMLInputElement>(".move-search")!;
    search.oninput = () => { query = search.value; render(); search.focus(); };

    dialog.querySelectorAll<HTMLElement>(".move-arrow").forEach(a =>
      a.onclick = (e) => {
        e.stopPropagation();
        const p = a.dataset.toggle!;
        if (query) return; // 搜索态展开由匹配决定
        expanded.has(p) ? expanded.delete(p) : expanded.add(p);
        render();
      });

    dialog.querySelectorAll<HTMLElement>(".move-node").forEach(n =>
      n.onclick = () => {
        const p = n.dataset.path!;
        if (p === item.category_path) return; // 当前位置不可选
        selected = p;
        render();
      });

    dialog.querySelector<HTMLElement>(".move-cancel")!.onclick = close;
    dialog.querySelector<HTMLElement>(".move-confirm")!.onclick = async () => {
      if (selected === null) return;
      const name = targetName();
      try {
        await api.mediaUpdate(item.id, item.category, selected, item.title,
          item.subtitle_path, item.cover_path, item.description);
        close();
        showToast(`已移动到「${name}」`);
        onMoved();
      } catch (e) {
        showToast("移动失败：" + e, "error");
      }
    };
  };

  overlay.onclick = (e) => { if (e.target === overlay) close(); };
  document.addEventListener("keydown", onKey, true);
  render();
  document.body.appendChild(overlay);
  dialog.querySelector<HTMLInputElement>(".move-search")?.focus();
}
```

- [ ] **Step 2: 追加样式到 `src/styles/theme.css` 末尾**

```css
/* 移动弹窗 */
.modal-overlay{position:fixed;inset:0;z-index:250;background:rgba(0,0,0,.45);
  display:flex;align-items:center;justify-content:center}
.move-dialog{width:420px;max-width:90vw;padding:18px;border-radius:12px}
.move-title{font-size:15px;color:var(--text);margin-bottom:12px}
.move-title b{color:#8fdca0}
.move-search{width:100%;box-sizing:border-box;padding:8px 12px;border-radius:9px;
  background:var(--glass);border:1px solid var(--border);color:var(--text);font-size:13px;margin-bottom:12px}
.move-tree{max-height:260px;overflow:auto;border:1px solid var(--border);border-radius:9px;padding:6px}
.move-node{display:flex;align-items:center;gap:6px;padding:6px 8px;border-radius:7px;
  font-size:13px;color:var(--text-dim);cursor:pointer;white-space:nowrap}
.move-node:hover{background:var(--glass)}
.move-node.sel{background:var(--accent-glow);color:var(--text)}
.move-node.cur{color:#6b7590;cursor:default}
.move-node.cur:hover{background:transparent}
.move-arrow{width:14px;display:inline-block;text-align:center;color:var(--text-dim)}
.move-tag{margin-left:8px;font-size:11px;color:#6b7590;border:1px solid #3a4260;border-radius:10px;padding:0 6px}
.move-hint{font-size:11px;color:var(--text-dim);margin:8px 2px 0}
.move-actions{display:flex;justify-content:flex-end;gap:10px;margin-top:14px}
.move-actions .btn.disabled{opacity:.4;cursor:default;pointer-events:none}
```

- [ ] **Step 3: 类型检查通过**

Run: `cd /Users/zhoumo/Documents/Claude/collector && npx tsc --noEmit`
Expected: 无错误（退出码 0）。

- [ ] **Step 4: 提交**

```bash
cd /Users/zhoumo/Documents/Claude/collector
git add src/components/MoveDialog.ts src/styles/theme.css
git commit -m "feat: MoveDialog 搜索+树弹窗组件

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: VideoView 接入弹窗，移除平铺菜单

**Files:**
- Modify: `src/views/VideoView.ts`（import 与「移动」菜单项）

- [ ] **Step 1: 改 import（`src/views/VideoView.ts` 第 3、6 行附近）**

把第 3 行
```ts
import { buildVideoTree, collectFolderPaths } from "../lib/videoTree";
```
改为
```ts
import { buildVideoTree } from "../lib/videoTree";
```
并在 import 组末尾（`import { openEditDrawer } ...` 下一行）新增
```ts
import { openMoveDialog } from "../components/MoveDialog";
```

- [ ] **Step 2: 替换「移动」菜单项（现第 36-59 行整块）**

把 `onContext` 中「移动」这一项对象整体：
```ts
      {
        label: "移动",
        onClick: () => {
          // 收集当前分类下所有文件夹节点（含分类根）作为移动目标
          const tree = buildVideoTree(activeCat, items.filter(i => i.category === activeCat));
          const folders = collectFolderPaths(tree);
          const menuItems = folders.map(fp => ({
            // 显示名：分类根（空串）显示分类名，其余显示相对分类的路径
            label: fp === "" ? `${activeCat}（根）` : fp,
            disabled: fp === it.category_path,
            onClick: async () => {
              if (fp === it.category_path) return;
              try {
                await api.mediaUpdate(it.id, it.category, fp, it.title,
                  it.subtitle_path, it.cover_path, it.description);
                refresh();
              } catch (e) {
                alert("移动失败：" + e);
              }
            },
          }));
          showContextMenu(x, y, menuItems);
        },
      },
```
替换为：
```ts
      {
        label: "移动",
        onClick: () => {
          const tree = buildVideoTree(activeCat, items.filter(i => i.category === activeCat));
          openMoveDialog(it, tree, refresh);
        },
      },
```

- [ ] **Step 3: 类型检查通过（确认无未使用 import 报错）**

Run: `cd /Users/zhoumo/Documents/Claude/collector && npx tsc --noEmit`
Expected: 无错误。特别确认没有 `collectFolderPaths` / `showContextMenu` 未使用的报错——`showContextMenu` 仍被 `onContext` 外层使用，保留；`collectFolderPaths` 已从 import 移除。

- [ ] **Step 4: 完整构建通过**

Run: `cd /Users/zhoumo/Documents/Claude/collector && npm run build`
Expected: `tsc` 与 `vite build` 均成功，无错误。

- [ ] **Step 5: 提交**

```bash
cd /Users/zhoumo/Documents/Claude/collector
git add src/views/VideoView.ts
git commit -m "feat: 移动菜单改用 MoveDialog 弹窗，移除平铺二级菜单

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: 真机验证

**Files:** 无（手动验收）

- [ ] **Step 1: 启动应用**

```bash
cd /Users/zhoumo/Documents/Claude/collector
lsof -ti:1420 | xargs kill -9 2>/dev/null; pkill -f "tauri dev" 2>/dev/null; sleep 1
nohup npm run tauri dev > /tmp/collector-dev.log 2>&1 & disown
```
等待编译完成（`tail -f /tmp/collector-dev.log` 看到窗口起来）。

- [ ] **Step 2: 逐项验收（在应用里操作）**

- 电影分类（108 文件夹）右键任一视频 → 「移动」→ 弹窗打开，默认只展开根一层。
- 搜索框输入关键词 → 树即时过滤，命中项祖先自动展开、无关分支隐藏；清空恢复全树。
- 点箭头能展开/折叠（无搜索时）。
- 点文件夹名 → 高亮选中，底部按钮变为「移动到「XX」」。
- 视频当前所在文件夹标「当前位置」、灰置、点击无效。
- 点「移动到此」→ 弹窗关闭 + 底部出现「已移动到「XX」」toast + 视频出现在目标文件夹（列表已刷新）。
- 把视频移到已存在同名条目的文件夹 → 出现红色「移动失败：…」toast，弹窗保持打开。
- 点「取消」/ 按 Esc / 点弹窗外遮罩 → 关闭且不移动。
- 切到动漫、剧集分类，重复关键路径一遍，确认通用。

- [ ] **Step 3: 关闭应用**

```bash
lsof -ti:1420 | xargs kill -9 2>/dev/null; pkill -f "tauri dev" 2>/dev/null
```

---

## 自审（对照 spec）

- **spec 覆盖**：搜索保留树结构→Task1 `matchTree` + Task3 渲染；点击确认→Task3 confirm；toast→Task2 + Task3 调用；当前位置灰置→Task3 `.cur`；根可选→Task3 `isRoot`；仅当前分类→Task4 传 `activeCat` 树；复用 mediaUpdate→Task3。全部有对应任务。
- **占位符**：无 TBD/TODO，每个代码步骤含完整代码。
- **类型一致**：`matchTree` 返回 `{visible:Set<string>, forceExpand:Set<string>}`（Task1 定义，Task3 消费一致）；`openMoveDialog(item, tree, onMoved)`、`showToast(message, kind?)` 签名跨任务一致；`api.mediaUpdate` 七参签名与 `ipc.ts` 一致。

## 风险

- `matchTree` 命中中间节点时令整枝可见（父命中→子全显），符合 spec「保留树结构」，已用单测覆盖。
- 移除 `collectFolderPaths` import 后若别处仍引用会报错——Task4 Step3 用 tsc 兜底（经查 VideoView 是唯一调用处）。
- toast/弹窗均纯前端，无后端与迁移风险。
